#pragma once

#include <string>

namespace Commands {

std::string start();
std::string premium();
std::string addBot();
std::string botList();
std::string botStats();
std::string botMin();
std::string botCast();

}
